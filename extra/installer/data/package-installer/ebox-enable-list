#!/usr/bin/perl

use EBox;
use EBox::Global;
use EBox::ServiceManager;

EBox::init();

my $global = EBox::Global->getInstance();

my $mgr = EBox::ServiceManager->new();

foreach my $modName (@{$mgr->_dependencyTree()}) {
    print "$modName\n";
}

$mgr->updateDigests();
