<%args>
    $module
</%args>
#!/bin/bash

set -e

#DEBHELPER#

case "$1" in
    configure)
        invoke-rc.d zentyal <% $module %> restart

        dpkg-trigger --no-await zentyal-core
    ;;
esac

exit 0
