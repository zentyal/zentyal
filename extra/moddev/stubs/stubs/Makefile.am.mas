<%args>
    $module
</%args>
## Process this file with automake to produce Makefile.in

Stubdir = @STUBSPATH@/<% $module %>

nobase_Stub_DATA = service.conf.mas

EXTRA_DIST = $(nobase_Stub_DATA)

MAINTAINERCLEANFILES = Makefile.in
