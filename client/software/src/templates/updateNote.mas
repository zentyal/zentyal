<%args>
$status
$qa
</%args>
<%init>
my $msg = "";
my $class = "note";

if ($qa) {
    $msg = __('As you are enjoying a Commercial Server Subscription the updates are Quality Assured by the eBox Development Team.');
    $class = 'note';
} else {
    $msg =  __x("Warning: The updates are community based. You can now gain access to the Quality Assured Updates by purchasing the <a href='{proUrl}'>Professional</a>  or <a href='{entUrl}'>Enterprise</a> Server Subscription. QA Updates guarantee that all the upgrades, bugfixes and security updates are extensively tested by the eBox Development Team and you won't be introducing any regressions on your already working system.",
            proUrl => 'http://store.ebox-technologies.com/serversubscriptions/subscription-professional.html',
            entUrl => 'http://store.ebox-technologies.com/serversubscriptions/ebox-enterprise-subscription.html',
            );
	$class = 'error';
}
</%init>
<div class='<% $class %>'>
<% $msg %>
</div>
