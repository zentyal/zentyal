#!/bin/bash

case "$1" in
	remove)
		kill `pidof gconfd-2` >/dev/null 2>&1 || true

		# openvpn changes network interfaces so we must restart network module to propagate the changes
                /etc/init.d/ebox network restart   || true 
		;;		
esac

#DEBHELPER#
