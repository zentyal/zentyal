<%init>
use EBox::Gettext;
my $networkUrl = '/ebox/Network/Ifaces';

my $adviceText = __('OpenVPN servers require at leat one external network
interface available. Configure one first');

my $text = "<a href=$networkUrl>$adviceText</a>";
</%init>
<div class='note'>
<% $text %>
</div>
