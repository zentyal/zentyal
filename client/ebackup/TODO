- Fix class naming. Most classes are named Remote* as we had local backup
  and remote backup. Now it's unified so it doesn't make to call them Remote*
  anymore
- Events are not working
- ebox-remote-backup has too much code that should be done in EBackup.pm
