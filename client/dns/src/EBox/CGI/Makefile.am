## Process this file with automake to produce Makefile.in

perldir = @PERLPATH@/EBox/CGI/DNS

nobase_perl_DATA = Index.pm Enable.pm AddDomain.pm DelDomain.pm Edit.pm AddHostname.pm DelHostname.pm AddAlias.pm DelAlias.pm

MAINTAINERCLEANFILES = Makefile.in

EXTRA_DIST = $(nobase_perl_DATA)
