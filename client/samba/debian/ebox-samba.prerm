#!/bin/bash


#DEBHELPER#

case "$1" in
	remove)
		# clean ldap
		#/usr/share/ebox-samba/ebox-samba-ldap clean
		
		#sed  -i -e 's/^\(passwd:.*\)ldap.*/\1/' /etc/nsswitch.conf
		#sed  -i -e 's/^\(group:.*\)ldap.*/\1/' /etc/nsswitch.conf
		#sed  -i -e 's/^\(shadow:.*\)ldap.*/\1/' /etc/nsswitch.conf
		;;	
	purge)
		#test -d /home/samba && rm -rf /home/samba
		;;
esac 
