#!/bin/bash

case "$1" in
	remove)
		kill `pidof gconfd-2` >/dev/null 2>&1 || true
		invoke-rc.d samba stop || true
		;;		
	purge)
	 	test -d /home/samba && rm -rf /home/samba

	        ;;    
esac

#DEBHELPER#
