#!/bin/bash

#DEBHELPER#

case "$1" in
	remove)
		kill `pidof gconfd-2` >/dev/null 2>&1 || true
		
		# restart logs
		invoke-rc.d ebox logs restart || true

        # Restart eBox apache
        invoke-rc.d ebox apache restart || true
		
		# /usr/share/ebox/ebox-clean-gconf printers || true
		;;		
	purge)
        # /usr/share/ebox/ebox-sql-table drop jobs || true

        ;;    
esac


