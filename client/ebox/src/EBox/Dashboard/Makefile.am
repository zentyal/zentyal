## Process this file with automake to produce Makefile.in

perldir = @PERLPATH@/EBox/Dashboard

nobase_perl_DATA = CounterGraph.pm Graph.pm GraphRow.pm Item.pm List.pm ModuleStatus.pm Section.pm Value.pm Widget.pm

MAINTAINERCLEANFILES = Makefile.in

EXTRA_DIST = $(nobase_perl_DATA)
