## Process this file with automake to produce Makefile.in
perldir = @PERLPATH@/EBox/CGI/ServiceModule

nobase_perl_DATA = ConfigureModuleController.pm ConfigureView.pm Controller.pm \
				   Makefile.am  StatusController.pm  StatusView.pm  View.pm 

SOURCES = $(nobase_perl_DATA)

MAINTAINERCLEANFILES = Makefile.in

EXTRA_DIST = $(nobase_perl_DATA)
