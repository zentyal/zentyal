#!/bin/bash

#DEBHELPER#

case "$1" in
	configure)
		dpkg-trigger --no-await ebox
		cp /var/lib/ebox/conf/ssl/ssl.pem /etc/jabberd2/ebox.pem
		chmod 640 /etc/jabberd2/ebox.pem
		chown jabber:jabber /etc/jabberd2/ebox.pem
esac

exit 0
