#!/bin/bash

#DEBHELPER#

case "$1" in
	remove)
		kill -9 `pidof gconfd-2` >/dev/null 2>&1 || true
		# Delete the issued certificates
		#rm -rf /var/lib/ebox/CA
		#/usr/share/ebox/ebox-clean-gconf ca || true
		;;		
esac


