#!/bin/bash

set -e

#DEBHELPER#

case "$1" in
	configure)
		kill -9 `pidof gconfd-2` >/dev/null 2>&1 || true
		# Migrate data if needed
		/usr/share/ebox/ebox-migrate /usr/share/ebox-ca/migration/
    ;;
esac


exit 0
