<!-- vim: ts=4 sw=4 nowrap filetype=mason
-->
<%args>
	$params
</%args>
<%init>
use EBox::Gettext;
</%init>
<div>
<br/>

%if($params->{'mail'} eq undef) {
	<& /mail/createaccount.mas, title => __('Create mail account'), params =>
		$params &>
%} else {
	<& /mail/alias.mas, title => __('Create mail alias to mail account'), params =>
		$params &>
%}
