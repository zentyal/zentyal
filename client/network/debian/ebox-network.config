#!/bin/sh

# Load debconf
. /usr/share/debconf/confmodule

if [  "$1" = "configure" -a "$2" = "0.11.99-0ubuntu1" ]; then
    db_input critical ebox-network/overwrite_ifaces || true

    db_go || true
fi
