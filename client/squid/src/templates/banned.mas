<!-- vim: ts=4 sw=4 filetype=mason
-->
<%args>
	@domains
        $globalPolicy => 'deny'
</%args>
<%init>
use EBox::Gettext;
</%init>

<& squid/domains.mas, title => __('Banned domains'), description =>
	__('Add here those domains which you do not want to allow access.'),
	action => 'Ban', domains => \@domains, globalPolicy => $globalPolicy &>
