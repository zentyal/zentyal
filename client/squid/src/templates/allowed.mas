<!-- vim: ts=4 sw=4 filetype=mason
-->
<%args>
	@domains
        $globalPolicy => 'deny'
</%args>
<%init>
use EBox::Gettext;
</%init>

<& squid/domains.mas, title => __('Allowed domains'), description =>
	__('Add here those domains which you want to allow access.'),
	action => 'Allow', domains => \@domains, globalPolicy => $globalPolicy &>
