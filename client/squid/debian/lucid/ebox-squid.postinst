#!/bin/bash

set -e

#DEBHELPER#

case "$1" in
    configure)
        # add ebox to proxy to read squid logs
        adduser --quiet ebox proxy || true

        # create log table
        /usr/share/ebox/ebox-sql-table add squid_access /usr/share/ebox/sqllog/squid_access.sql
        /usr/share/ebox/ebox-sql-table add squid_access_report /usr/share/ebox/sqllog/squid_access_report.sql
        /usr/share/ebox/ebox-sql-table-with-time-period add squid_traffic /usr/share/ebox/sqllog/squid_traffic.sql
        # migrate data if needed
        /usr/share/ebox/ebox-migrate /usr/share/ebox-squid/migration/ || true

        # directory for extra dg domain lists
        DG_EXTRALISTS=/etc/dansguardian/extralists
        test -d $DG_EXTRALISTS || mkdir -p -m 0755 $DG_EXTRALISTS

        # restart logs
        invoke-rc.d ebox logs restart || true
        # restart squid module
        invoke-rc.d ebox squid restart || true
        # restart firewall module
        invoke-rc.d ebox firewall restart || true

        dpkg-trigger --no-await ebox
    ;;

esac



exit 0
