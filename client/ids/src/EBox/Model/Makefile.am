## Process this file with automake to produce Makefile.in

perldir = @PERLPATH@/EBox/IDS/Model

SUBDIRS = Report

mods = Interfaces.pm Rules.pm

nobase_perl_DATA = $(mods)

SOURCES = $(nobase_perl_DATA)

EXTRA_DIST = $(mods)

MAINTAINERCLEANFILES = Makefile.in
