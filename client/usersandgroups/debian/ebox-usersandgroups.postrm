#!/bin/bash

onRemove()
{
       perl -e 'use EBox::UsersAndGroups; EBox::UsersAndGroups::onRemove();'
	
}

case "$1" in
	remove)
		kill `pidof gconfd-2` >/dev/null 2>&1 || true
		onRemove();
		/usr/bin/ebox-clean-gconf usersandgroups || true
		;;		
esac

#DEBHELPER#
