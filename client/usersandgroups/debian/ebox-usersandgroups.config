#! /bin/sh

set -e

# Load debconf
. /usr/share/debconf/confmodule

# Check if the user wants to keep the LDAP database

db_input medium ebox-usersandgroups/keep_database || true
db_go || true


exit 0

