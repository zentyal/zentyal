class: 'EBox::TrafficShaping'

depends:
    - network
    - objects
    - services

enabledepends:
    - network
    - firewall

models:
    - InternalRules
    - ExternalRules
    - InterfaceRate

composites:
    Rules: [InternalRules, ExternalRules]

notifyactions:
    RuleTable: InterfaceRate
