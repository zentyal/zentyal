class: 'EBox::TrafficShaping'

depends:
    - network
    - objects
    - services

enabledepends:
    - network
    - firewall

composites:
    DynamicGeneral: [FIXME]

notifyactions:
    RuleTable: InterfaceRate
