<%args>
    @users
</%args>
# Secrets for authentication using CHAP
# client	server	secret		IP addresses
#username	pptpd	password	*

%    foreach my $user (@users) {
%    $user->{'ipaddr'} = '*' unless $user->{'ipaddr'};
<% $user->{'user'} %>	pptpd	<% $user->{'passwd'} %>	<% $user->{'ipaddr'} %>
%    }
