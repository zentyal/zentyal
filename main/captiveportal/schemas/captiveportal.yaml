class: 'EBox::CaptivePortal'

depends:
    - objects
    - network
    - firewall
    - users

enabledepends:
    - firewall
    - users

models:
    - Interfaces
    - Settings
    - BWSettings
    - Users
    - CaptiveUser
    - SecondaryLDAP

composites:
    GeneralSettings: [Settings, BWSettings, Interfaces]
    General: [GeneralSettings, Users]
    GeneralWithLDAP: [GeneralSettings, SecondaryLDAP, Users]
