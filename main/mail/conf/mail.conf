# mail.conf - configuration file for zentyal-mail
#
# Everything after a '#' character is ignored
#
# All whitespace is ignored
#
# Config keys are set this way:
#
# key = value
#
# They may contain comments at the end:
#
# key = value # this is ignored

# -- broker settings for dovecot openchange plugin --
dovecot_oc_plugin_broker_host = localhost
dovecot_oc_plugin_broker_port = 5672
dovecot_oc_plugin_broker_user = guest
dovecot_oc_plugin_broker_pass = guest
dovecot_oc_plugin_broker_vhost =
dovecot_oc_plugin_broker_exchange = openchange
dovecot_oc_plugin_broker_routing_key = dovecot-new-mail
