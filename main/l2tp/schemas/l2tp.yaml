class: 'EBox::L2TP'
enabledepends:
    - samba

depends:
    - network
    - firewall
    - samba

models:
    - Connections
    - ConnectionSettings
    - RangeTable

foreign:
    - Connections: [RangeTable]
