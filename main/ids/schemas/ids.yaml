class: 'EBox::IDS'

depends:
    - network

enabledepends:
    - network

models:
    - Interfaces
    - Rules
    - Report::AlertDetails
    - Report::AlertGraph
    - Report::AlertReportOptions

composites:
    General: [ids/Interfaces, ids/Rules]
    Report::AlertReport: [AlertReportOptions, AlertGraph, AlertDetails]
