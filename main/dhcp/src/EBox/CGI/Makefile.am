## Process this file with automake to produce Makefile.in

perldir = @PERLPATH@/EBox/CGI/DHCP

nobase_perl_DATA = Index.pm Status.pm AddRange.pm AddFixed.pm RemoveRange.pm \
				Enable.pm RemoveFixed.pm SetIfaceOptions.pm

MAINTAINERCLEANFILES = Makefile.in

EXTRA_DIST = $(nobase_perl_DATA)
