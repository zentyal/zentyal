#!/bin/bash

case "$1" in
    remove)
        dpkg-trigger --no-await zentyal-core
        ;;

    purge)
        # Purge configuration
        /usr/share/zentyal/clean-conf dhcp

        # drop tables
        /usr/share/zentyal/drop-tables dhcp
        ;;
esac

exit 0
