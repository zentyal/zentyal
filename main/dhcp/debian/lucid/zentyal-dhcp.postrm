#!/bin/bash

set -e

case "$1" in
    remove)
        dpkg-trigger --no-await zentyal-core
        ;;

    purge)
        # Purge configuration
        /usr/share/zentyal/clean-conf dhcp || true

        # drop log table
        /usr/share/zentyal/sql-table drop leases || true
        ;;
esac
