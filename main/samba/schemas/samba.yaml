class: 'EBox::Samba'

depends:
    - network
    - users

enabledepends:
    - network
    - users
    - dns

models:
    - GeneralSettings
    - SambaShares
    - SambaSharePermissions
    - SambaDeletedShares

composites:
    General: [samba/GeneralSettings, SambaShares]
