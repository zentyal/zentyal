## Process this file with automake to produce Makefile.in

SUBDIRS = sqllog

pkgdata_SCRIPTS = initial-setup enable-module asterisk-ldap detect-system-kernels.pl

MAINTAINERCLEANFILES = Makefile.in

EXTRA_DIST = $(pkgdata_SCRIPTS)
