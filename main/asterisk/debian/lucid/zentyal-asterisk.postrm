#!/bin/bash

case "$1" in
    remove)
        dpkg-trigger --no-await zentyal-core
    ;;
    purge)
        # Purge configuration
        /usr/share/zentyal/clean-conf asterisk || true

        /usr/share/zentyal/sql-table drop asterisk_cdr || true
    ;;
esac

exit 0
