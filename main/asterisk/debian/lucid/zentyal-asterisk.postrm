#!/bin/bash

case "$1" in
    remove)
        dpkg-trigger --no-await zentyal-core
    ;;
    purge)
        # Purge configuration
        /usr/share/zentyal/clean-conf asterisk

        /usr/share/zentyal/drop-tables asterisk
    ;;
esac

exit 0
