pre-start script
        invoke-rc.d asterisk stop || true
        if ! test -d /var/run/asterisk; then
            mkdir /var/run/asterisk
            chown asterisk:asterisk /var/run/asterisk
        fi
end script

exec /usr/sbin/asterisk -f -U asterisk
respawn
