#!/bin/bash

set -e

#DEBHELPER#

case "$1" in
	configure)
        # Migrate data if needed
        /usr/share/zentyal/ebox-migrate /usr/share/zentyal-ca/migration/

        dpkg-trigger --no-await zentyal-core
    ;;
esac

exit 0
