## Process this file with automake to produce Makefile.in

Stubdir = @STUBSPATH@/dns

nobase_Stub_DATA = db.mas dbrev.mas keys.mas named.conf.mas \
		   named.conf.options.mas named.conf.local.mas

EXTRA_DIST = $(nobase_Stub_DATA)

MAINTAINERCLEANFILES = Makefile.in
