#!/bin/bash

if [ "$1" = "purge" ]; then
        # Purge configuration
        /usr/share/zentyal/zentyal-clean-conf l7-protocols || true
fi

#DEBHELPER#

case "$1" in
	remove)
        dpkg-trigger --no-await zentyal-core
		;;
esac


