#!/bin/bash

set -e

#DEBHELPER#

case "$1" in
	configure)

        # Migrate data if needed
        /usr/share/zentyal-core/ebox-migrate /usr/share/zentyal-services/migration/ || true

        dpkg-trigger --no-await zentyal-core
     ;;
esac



exit 0
