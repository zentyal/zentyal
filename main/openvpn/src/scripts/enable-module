#!/bin/bash

set -e

cd /etc/frr/

# check required files
for conf in zebra.conf ripd.conf; do
    if [ ! -f $conf ]; then
        touch $conf
    fi
    chmod 0640 $conf
    chown frr:frr $conf
done
