## Process this file with automake to produce Makefile.in

Stubdir = @STUBSPATH@/zarafa

nobase_Stub_DATA = ical.cfg.mas indexer.cfg.mas gateway.cfg.mas \
                   monitor.cfg.mas server.cfg.mas ldap.openldap.cfg.mas \
                   spooler.cfg.mas dagent.cfg.mas apache.mas

EXTRA_DIST = $(nobase_Stub_DATA)

MAINTAINERCLEANFILES = Makefile.in
