## Process this file with automake to produce Makefile.in

perldir = @PERLPATH@/EBox/Zarafa/Model

nobase_perl_DATA = VMailDomain.pm GeneralSettings.pm Gateways.pm Quota.pm ZarafaUser.pm

DISTCLEANFILES =

EXTRA_DIST = $(nobase_perl_DATA)

MAINTAINERCLEANFILES = Makefile.in
