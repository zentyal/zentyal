#!/bin/bash

if [ "$1" = "purge" ]; then
        # Purge configuration
        /usr/share/zentyal/clean-conf jabber || true
fi

case "$1" in
	remove)
            dpkg-trigger --no-await zentyal-core
        ;;
esac

exit 0
