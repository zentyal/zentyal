#!/bin/bash

#DEBHELPER#

case "$1" in
	configure)
        # migrate data if needed
        /usr/share/zentyal/ebox-migrate /usr/share/zentyal-jabber/migration/

        # restart module
        invoke-rc.d zentyal jabber restart

	    dpkg-trigger --no-await zentyal-core
        ;;
esac

exit 0
