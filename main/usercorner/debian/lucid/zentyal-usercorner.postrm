#!/bin/bash

case "$1" in
    remove)
        dpkg-trigger --no-await zentyal-core
    ;;
    remove)
        # Purge configuration
        /usr/share/zentyal/clean-conf usercorner
    ;;
esac

exit 0
