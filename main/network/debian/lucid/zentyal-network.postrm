#!/bin/bash

set -e

if [ "$1" = "purge" ]; then
        # Purge configuration
        /usr/share/zentyal/clean-conf network || true
fi

case "$1" in
	remove)
        dpkg-trigger --no-await zentyal-core
        ;;
esac

