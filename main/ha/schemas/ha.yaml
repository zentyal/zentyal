class: EBox::HA

depends:
    - network

enabledepends:
    - network

models:
    - Cluster
    - FloatingIP
    - ClusterMetadata
    - ClusterState

composites:
    HA: [Cluster, FloatingIP, ClusterMetadata]
