class: EBox::HA

depends:
    - network

enabledepends:
    - network

models:
    - Cluster
    - FloatingIP
    - ClusterMetadata
    - ClusterState
    - Nodes
    - NodeStatus
    - ResourceStatus
    - ClusterStatusSummary
    - Errors

composites:
    Initial: [Cluster, FloatingIP]
    General: [Configuration, Status]
    Status: [ClusterStatusSummary, NodeStatus, ResourceStatus, Errors]
    Configuration: [ClusterMetadata, Nodes, FloatingIP, Cluster]
