class: EBox::HA

depends:
    - network

enabledepends:
    - network

models:
    - Cluster
    - FloatingIP
    - ClusterMetadata
    - ClusterState
    - Nodes
    - NodeStatus
    - ResourceStatus

composites:
    Initial: [Cluster, FloatingIP]
    General: [Configuration, Status]
    Status: [NodeStatus, ResourceStatus]
    Configuration: [ClusterMetadata, Nodes, FloatingIP, Cluster]
