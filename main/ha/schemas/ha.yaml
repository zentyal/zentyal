class: EBox::HA

depends:
    - network

enabledepends:
    - network

models:
    - Cluster
    - FloatingIP
    - ClusterMetadata
    - ClusterState

composites:
    Initial: [Cluster, FloatingIP]
    Configuration: [ClusterMetadata, FloatingIP, Cluster]
