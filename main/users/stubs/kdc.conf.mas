<%args>
    $ldapContainer
</%args>
[logging]
    kdc = FILE:/var/log/heimdal-kdc.log

[kdc]
    database = {
        dbname = ldap:<% $ldapContainer %>
        acl_file = /var/lib/heimdal-kdc/kadmind.acl
    }

[kadmin]

[password-quality]

