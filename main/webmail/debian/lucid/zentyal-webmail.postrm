#!/bin/bash

if [ "$1" = "purge" ]; then
        # Purge configuration
        /usr/share/zentyal/clean-conf webmail || true
fi

case "$1" in
	remove)
        dpkg-trigger --no-await zentyal-core
        ;;

    purge)
        ;;
esac
