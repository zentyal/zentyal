#!/bin/bash

#DEBHELPER#

case "$1" in
    configure)
        # Migrate data if needed
        /usr/share/zentyal-core/ebox-migrate /usr/share/ebox-radius/migration/

        dpkg-trigger --no-await zentyal-core
    ;;
esac

exit 0
