#!/bin/bash

#DEBHELPER#

case "$1" in
    configure)
        # Migrate data if needed
        /usr/share/ebox/ebox-migrate /usr/share/ebox-radius/migration/

        dpkg-trigger --no-await ebox
    ;;
esac

exit 0
