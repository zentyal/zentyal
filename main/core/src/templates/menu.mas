<%args>
@items
$current => 'none'
$currentUrl => ''
$copyright_footer
</%args>
<div id='menu'>

<div class="menusearch">
  <form action='/Search'>
     <input name="search" id="menusearch" type="text" placeholder="<% __('Search') %>"/>
     <input type="submit"/>
  </form>
</div>
<ul id='nav'>
% foreach my $item (@items) {
        <% $item->html($current, $currentUrl) %>
% }
</ul>
    <div id="footer">
        <% $copyright_footer %>
    </div>
</div>

