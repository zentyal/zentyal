<%args>
    $fieldName
    $JSONActions
    $tableName
</%args>
<%init>
my $fullId = "${tableName}_${fieldName}";
</%init>
jQuery('#<% $fullId %>').change(function (event) {
              onFieldChange(event, <% $JSONActions %>, '<% $tableName %>')
}  );

