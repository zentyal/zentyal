## Process this file with automake to produce Makefile.in

templatedir = $(datadir)/ebox/templates/common
template_DATA = enable.mas

EXTRA_DIST = $(template_DATA)

MAINTAINERCLEANFILES = Makefile.in
