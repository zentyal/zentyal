<%args>
    $title
    $num
    @paragraphs => ()
    @items => ()
    @links => ()
    $endText => ''
</%args>
<div class='ads'>
<h2><% $title %></h2>

<img src="/data/software/images/slide<% $num %>.png">

% foreach my $text (@paragraphs) {
<p><% $text %></p>
% }

% if (@items) {
<ul>
%  foreach my $item (@items) {
    <li><% $item %></li>
%  }
</ul>
% }

% foreach my $link (@links) {
<div class="ads_link"><a onclick="window.open('<% $link %>)'"><% $link %></a></div>
% }

</div>
