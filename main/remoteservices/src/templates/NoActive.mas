<%init>
use EBox::Gettext;
use EBox::Config;

my $text = __x('This feature is only available if you register your server. '
               . 'You can register it at {oh}register page{ch}.',
               oh => '<a href="/Wizard?page=RemoteServices/Wizard/Subscription">',
               ch => '</a>');
</%init>
<& /msg.mas, msg => $text &>
