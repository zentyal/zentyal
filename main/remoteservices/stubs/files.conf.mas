<%doc>
  Files sync client configuration file
</%doc>
<%args>
    $store_dir
    $access_id
    $access_key
    @folders => ()
</%args>
{
    "remote_host" : "ec2-54-247-155-61.eu-west-1.compute.amazonaws.com",
    "remote_port" : "4433",
    "store_dir" : "<% $store_dir %>",
    "access_id" : "<% $access_id %>",
    "access_key" : "<% $access_key %>",
    "shares" : [
% foreach my $folder (@folders) {
        {
            "local_path" : "<% $folder->path() %>",
            "remote_path" : "<% $folder->type() %>s/<% $folder->name() %>/"
% # add a coma after the dict
%   if (\$folder == \$folders[-1]) {
        }
%   } else {
        },
%   }
% }
    ]
}

